/* RADIO UNDERGROUND v2 — core/font_ext.js
   Glyphs Build 004 never needed. font.js is a VERBATIM port and stays that
   way, so extensions live here — the same split as audio.js / voices.js.

   text() falls back to FONT['?'] for anything it doesn't know, which is why
   the exit toast rendered as "EXIT -?" instead of "EXIT ->".

   Column bits, 5 wide: col0=16 col1=8 col2=4 col3=2 col4=1. */
FONT['>']=[8,4,2,1,2,4,8];
FONT['<']=[2,4,8,16,8,4,2];
FONT['=']=[0,0,31,0,31,0,0];
FONT['_']=[0,0,0,0,0,0,31];
