Drop raw renders here, named EXACTLY the shot-list ID:
    STORE-EXT-A.png     (not "storefront final v3.png")
The filename becomes the room ID everywhere else in the project.

Then:  py tools\intake.py assets\backdrops\raw\STORE-EXT-A.png
